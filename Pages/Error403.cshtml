@page
@model OfficeVersionsCore.Pages.Error403Model
@{
    ViewData["Title"] = "Access Forbidden";
}

<div class="error-page-container">
    <div class="error-content">
        <div class="error-icon-wrapper">
            <div class="error-icon error-403">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
                    <path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
                </svg>
            </div>
        </div>
        
        <h1 class="error-code">403</h1>
        <h2 class="error-title">Access Forbidden</h2>
        <p class="error-message">
            Spiacente, ma non hai i permessi necessari per accedere a questa risorsa.
            Se pensi che questo sia un errore, contattaci per ricevere assistenza.
        </p>
        
        <div class="error-suggestions">
            <h3>Possibili motivi:</h3>
            <ul>
                <li><i class="bi bi-shield-lock"></i> La risorsa richiede un'autenticazione</li>
                <li><i class="bi bi-person-x"></i> Il tuo account non ha i permessi necessari</li>
                <li><i class="bi bi-clock-history"></i> La tua sessione potrebbe essere scaduta</li>
                <li><i class="bi bi-exclamation-triangle"></i> L'accesso a questa risorsa è limitato</li>
            </ul>
            
            <h3 class="mt-3">Cosa puoi fare:</h3>
            <ul>
                <li><i class="bi bi-arrow-left-circle"></i> Torna alla pagina precedente</li>
                <li><i class="bi bi-house-door"></i> Vai alla <a asp-page="/Index">Homepage</a></li>
                <li><i class="bi bi-envelope"></i> <a asp-page="/Contact">Contattaci</a> se hai bisogno di aiuto</li>
            </ul>
        </div>
        
        <div class="error-actions">
            <a asp-page="/Index" class="btn btn-primary btn-lg">
                <i class="bi bi-house-door"></i> Home
            </a>
            <button onclick="history.back()" class="btn btn-outline-secondary btn-lg">
                <i class="bi bi-arrow-left"></i> Go Back
            </button>
        </div>
    </div>
</div>

<style>
    .error-page-container {
        min-height: 70vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem 1rem;
        text-align: center;
    }
    
    .error-content {
        max-width: 600px;
        animation: fadeInUp 0.6s ease-out;
    }
    
    .error-icon-wrapper {
        margin-bottom: 2rem;
    }
    
    .error-icon {
        width: 120px;
        height: 120px;
        margin: 0 auto;
        color: #ffc107;
        animation: lockShake 1s ease-in-out;
    }
    
    .error-icon svg {
        width: 100%;
        height: 100%;
    }
    
    .error-code {
        font-size: 6rem;
        font-weight: 700;
        color: #ffc107;
        margin: 0;
        line-height: 1;
        background: linear-gradient(135deg, #ffc107 0%, #ffb300 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }
    
    .error-title {
        font-size: 2rem;
        font-weight: 600;
        color: var(--bs-dark, #212529);
        margin: 1rem 0;
    }
    
    .error-message {
        font-size: 1.125rem;
        color: var(--bs-secondary, #6c757d);
        margin-bottom: 2rem;
        line-height: 1.6;
    }
    
    .error-suggestions {
        background: var(--bs-light, #f8f9fa);
        border-radius: 12px;
        padding: 1.5rem;
        margin-bottom: 2rem;
        text-align: left;
    }
    
    .error-suggestions h3 {
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--bs-dark, #212529);
        margin-bottom: 1rem;
    }
    
    .error-suggestions ul {
        list-style: none;
        padding: 0;
        margin: 0;
    }
    
    .error-suggestions li {
        padding: 0.5rem 0;
        color: var(--bs-dark, #212529);
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }
    
    .error-suggestions li i {
        color: #ffc107;
        font-size: 1.25rem;
    }
    
    .error-suggestions a {
        color: var(--bs-primary, #0066cc);
        text-decoration: none;
        font-weight: 500;
    }
    
    .error-suggestions a:hover {
        text-decoration: underline;
    }
    
    .error-actions {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
    }
    
    .error-actions .btn {
        min-width: 140px;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
    }
    
    @@keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
    
    @@keyframes lockShake {
        0%, 100% { transform: rotate(0deg); }
        10%, 30%, 50%, 70%, 90% { transform: rotate(-5deg); }
        20%, 40%, 60%, 80% { transform: rotate(5deg); }
    }
    
    @@media (max-width: 768px) {
        .error-code {
            font-size: 4rem;
        }
        
        .error-title {
            font-size: 1.5rem;
        }
        
        .error-icon {
            width: 80px;
            height: 80px;
        }
    }
</style>
